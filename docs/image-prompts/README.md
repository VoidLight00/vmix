# Image prompts

The PNGs in `assets/` were generated from these prompts (one warm accent, no text inside images).
`assets/prompts/` holds the generic prompts the release tooling regenerates on every run.
